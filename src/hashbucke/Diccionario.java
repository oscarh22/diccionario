
package hashbucke;


public class Diccionario extends HashBucke{
    //llenarTabla();
    
    /*
    public void insertarDicc(String palabra, String traduccion){
        palabra = palabra.toLowerCase();
        int pos = hash (palabra);
        tabla[pos].add(palabra + ":" + traduccion);
    }
    
    public String biscarDic(String palabra){
        palabra = palabra.toLowerCase();
        int pos = hash(palabra);
        String [] cads = { null , null};
        Iterable<String>[] tabla;
        for(String el: tabla[pos]){
            if(el.contains(palabra))
                cads = el.split(" : ");
        }
        return cads[1];
    }*/
    
}
